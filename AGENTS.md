每次完成任务，都在创建任务日志。文件路径是 `docs/logs/YYYYMMDD-中文任务名.md`

## 关键目录结构

- `docs/` 项目文档
- `docs/logs/` 任务日志
- `scripts/` 脚本
- `src/` pocketstack 项目源代码
- `src/modules/` pocketstack 模块

## skills

skills存放在 `.skills`目录下：

- `pocketstack`： PocketStack 全栈开发框架，基于 React + shadcn/ui + PocketBase
